// Configuration file for obstacle settings
const OBSTACLE_CONFIG = {
    // Time intervals between obstacles (in milliseconds)
    MIN_OBSTACLE_INTERVAL: 1500,  // 1.5 seconds
    MAX_OBSTACLE_INTERVAL: 4000,  // 4 seconds
    
    // Bird positioning settings
    BIRD_MIN_HEIGHT: 80,    // Minimum height above ground for birds (pixels)
    BIRD_MAX_HEIGHT: 150,   // Maximum height above ground for birds (pixels)
    
    // Ground obstacle settings
    GROUND_OBSTACLE_MIN_HEIGHT: 40,  // Minimum height for ground obstacles
    GROUND_OBSTACLE_MAX_HEIGHT: 80,  // Maximum height for ground obstacles
};